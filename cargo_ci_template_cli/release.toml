allow-branch = ["main"]
pre-release-hook = [
  "git-cliff",
  "-o",
  "CLI_CHANGELOG.md",
  "--tag",
  "{{version}}",
]
tag-message = "chore(release): {{version}}"
tag-name = "v{{version}}"
pre-release-commit-message = "chore(release): {{version}}"
shared-version = true
release = true
tag = true
